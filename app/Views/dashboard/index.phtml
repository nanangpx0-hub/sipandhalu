<?php
$title = 'Dasbor';
$user = $user ?? null;
require __DIR__ . '/../layouts/header.phtml';
?>
<div class="d-flex justify-content-between align-items-center flex-wrap">
  <h1 class="h3 mb-0 text-gray-800"><i class="fas fa-tachometer-alt mr-2 text-primary"></i>Dasbor</h1>
  <span class="text-muted small"><i class="far fa-calendar-alt mr-1"></i><?= date('d M Y') ?></span>
</div>
<p class="text-muted mt-1 mb-3">Selamat datang, <b><?= \App\Core\View::e($user['nama'] ?? '') ?></b> <span class="badge badge-primary ml-1"><?= \App\Core\View::e($user['role_code'] ?? '') ?></span></p>

<?php if (!empty($success)): ?><div class="alert alert-success alert-dismissible fade show"><i class="fas fa-check-circle mr-2"></i><?= \App\Core\View::e($success) ?><button type="button" class="close" data-dismiss="alert">&times;</button></div><?php endif; ?>
<?php if (!empty($error)): ?><div class="alert alert-danger alert-dismissible fade show"><i class="fas fa-exclamation-circle mr-2"></i><?= \App\Core\View::e($error) ?><button type="button" class="close" data-dismiss="alert">&times;</button></div><?php endif; ?>

<div class="row">
  <div class="col-12 col-md-6 col-xl-3">
    <div class="info-box shadow-sm mb-3">
      <span class="info-box-icon bg-success"><i class="fas fa-users"></i></span>
      <div class="info-box-content">
        <span class="info-box-text">Petugas (pool orang)</span>
        <span class="info-box-number"><?= (int) ($ringkas['orang_total'] ?? 0) ?> <small class="text-muted">total · <?= (int) ($ringkas['orang_aktif'] ?? 0) ?> aktif</small></span>
        <a href="/petugas" class="small">Kelola <i class="fas fa-arrow-circle-right"></i></a>
      </div>
    </div>
  </div>
  <div class="col-12 col-md-6 col-xl-3">
    <div class="info-box shadow-sm mb-3">
      <span class="info-box-icon bg-indigo"><i class="fas fa-user-shield"></i></span>
      <div class="info-box-content">
        <span class="info-box-text">Akun login</span>
        <span class="info-box-number"><?= (int) ($ringkas['user_total'] ?? 0) ?> <small class="text-muted">total · <?= (int) ($ringkas['user_aktif'] ?? 0) ?> aktif</small></span>
        <?php if (($user['role_code'] ?? '') === 'ADMIN'): ?><a href="/users" class="small">Kelola <i class="fas fa-arrow-circle-right"></i></a><?php endif; ?>
      </div>
    </div>
  </div>
  <?php if (($user['role_code'] ?? '') !== 'PENGOLAH'): ?>
  <div class="col-12 col-md-6 col-xl-3">
    <div class="info-box shadow-sm mb-3">
      <span class="info-box-icon bg-teal"><i class="fas fa-map-marked-alt"></i></span>
      <div class="info-box-content">
        <span class="info-box-text">Master SLS</span>
        <span class="info-box-number"><?= (int) ($ringkas['sls_total'] ?? 0) ?> <small class="text-muted">total · <?= (int) ($ringkas['sls_aktif'] ?? 0) ?> aktif</small></span>
        <a href="/sls" class="small">Kelola <i class="fas fa-arrow-circle-right"></i></a>
      </div>
    </div>
  </div>
  <div class="col-12 col-md-6 col-xl-3">
    <div class="info-box shadow-sm mb-3">
      <span class="info-box-icon bg-warning"><i class="fas fa-calendar-alt"></i></span>
      <div class="info-box-content">
        <span class="info-box-text">Periode</span>
        <span class="info-box-number"><?= (int) ($ringkas['periode_total'] ?? 0) ?> <small class="text-muted"><?= (int) ($ringkas['periode_aktif'] ?? 0) ?> aktif · <?= (int) ($ringkas['periode_draft'] ?? 0) ?> draft</small></span>
        <a href="/periode" class="small">Kelola <i class="fas fa-arrow-circle-right"></i></a>
      </div>
    </div>
  </div>
  <?php endif; ?>
</div>

<?php
$badgeAksi = ['CREATE' => 'badge-success', 'UPDATE' => 'badge-primary', 'DELETE' => 'badge-danger'];
$iconAksi = ['CREATE' => 'fa-plus-circle', 'UPDATE' => 'fa-pen', 'DELETE' => 'fa-trash'];
?>
<div class="card shadow-sm">
  <div class="card-header border-0">
    <h3 class="card-title font-weight-bold"><i class="fas fa-history mr-1 text-muted"></i>Aktivitas Terakhir</h3>
    <div class="card-tools"><span class="badge badge-light border text-muted">10 terbaru</span></div>
  </div>
  <div class="card-body p-0 table-responsive">
    <table class="table table-striped table-hover table-valign-middle data-table">
      <thead class="thead-light"><tr><th>Waktu</th><th>User</th><th class="text-center">Aksi</th><th>Tabel</th><th class="text-center">ID</th></tr></thead>
      <tbody>
      <?php foreach ($logs as $l): ?>
      <tr>
        <td class="text-muted text-nowrap"><i class="far fa-clock mr-1"></i><?= \App\Core\View::e($l['created_at']) ?></td>
        <td><b><?= \App\Core\View::e($l['nama_user'] ?? ('#' . $l['user_id'])) ?></b></td>
        <td class="text-center"><span class="badge <?= $badgeAksi[$l['aksi']] ?? 'badge-secondary' ?>"><i class="fas <?= $iconAksi[$l['aksi']] ?? 'fa-circle' ?> mr-1"></i><?= \App\Core\View::e($l['aksi']) ?></span></td>
        <td><span class="badge badge-light border"><?= \App\Core\View::e($l['tabel_target']) ?></span></td>
        <td class="text-center text-muted"><?= \App\Core\View::e((string) $l['id_target']) ?></td>
      </tr>
      <?php endforeach; ?>
      <?php if (empty($logs)): ?>
      <tr><td colspan="5" class="text-center text-muted py-4"><i class="fas fa-inbox fa-2x d-block mb-2"></i>Belum ada aktivitas tercatat.</td></tr>
      <?php endif; ?>
      </tbody>
    </table>
  </div>
  <div class="card-footer text-muted small"><i class="fas fa-info-circle mr-1"></i>Semua perubahan data terekam otomatis pada audit trail.</div>
</div>
<?php require __DIR__ . '/../layouts/footer.phtml'; ?>
